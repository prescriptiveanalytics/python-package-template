// For format details, see https://aka.ms/devcontainer.json. For config options, see the
// README at: https://github.com/devcontainers/templates/tree/main/src/docker-existing-dockerfile
{
    "name": "{{ cookiecutter.__package_name_kebab_case }} DevContainer",
    "dockerComposeFile": "./docker-compose.yml",
    "service": "devcontainer",
    "workspaceFolder": "/workspace",
    "features": {
        // https://containers.dev/features
        // "ghcr.io/devcontainers/features/git-lfs:1": {
        //     "autoPull": true,
        //     "version": "latest"
        // }
        // support for vendoring agentic skills via npx skills
        "ghcr.io/devcontainers/features/node:2": { "version": "lts" }
    },
    "customizations": {
        "vscode": {
            "extensions": [
                // add extensions for vscode here, you can copy the id from the extension page
                // "ms-toolsai.jupyter",
                // "ms-toolsai.vscode-jupyter-powertoys",
                "ms-azuretools.vscode-docker",
                "ms-python.python",
                "ms-python.debugpy"
            ],
            "settings": {
                "terminal.integrated.shell.linux": "/bin/bash",
                "python.defaultInterpreterPath": "/opt/venv/bin/python",
                "python.venvFolders": [
                    // Autodiscover uv envs,
                    // set in Dockerfile or docker-compose.yml as well 
                    // "/opt/venv"
                ]
            }
        }
    }

    // Use 'forwardPorts' to make a list of ports inside the container available locally.
    // "forwardPorts": [],

    // Uncomment the next line to run commands after the container is created.
    // "postCreateCommand": "cat /etc/os-release",

    // Uncomment to connect as an existing user other than the container default. More info: https://aka.ms/dev-containers-non-root.
    // "remoteUser": "devcontainer"
}
